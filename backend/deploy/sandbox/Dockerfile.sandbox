# Minimal sandbox image for compiling and running C code
FROM gcc:13-bookworm

RUN apt-get update && apt-get install -y --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -s /bin/sh sandbox
USER sandbox
WORKDIR /workspace
